{
  "name": "auto-industry-tycoon",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @ait/web run dev",
    "dev:api": "npm --workspace @ait/api run start:dev",
    "build": "npm run build -ws",
    "typecheck": "npm run typecheck -ws"
  }
}
