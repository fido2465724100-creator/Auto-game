# Seed Notes

The current MVP uses TypeScript seed constants in `/data`.

TODO:
- implement executable Prisma seed script that inserts regions, technologies, events, and component metadata
- add migration + bootstrap command (`prisma migrate dev` + `prisma db seed`)
